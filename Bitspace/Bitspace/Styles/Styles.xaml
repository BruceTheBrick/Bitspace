<?xml version="1.0" encoding="utf-8" ?>
<ResourceDictionary
    x:Class="Bitspace.Styles.Styles"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:bitspace="clr-namespace:Bitspace">

    <!--  Colors  -->
    <bitspace:ColorRef x:Key="ColorPrimary" Color="{StaticResource Dark}" />
    <bitspace:ColorRef x:Key="ColorSecondary" Color="{StaticResource Blue}" />
    <bitspace:ColorRef x:Key="BackgroundColor" Color="{StaticResource Dark}" />
    <bitspace:ColorRef x:Key="TextColor" Color="{StaticResource Green}" />
    <bitspace:ColorRef x:Key="LoadingColor" Color="{StaticResource DarkThemeSkeletonLoader}" />


    <!--  ContentPage  -->
    <Style ApplyToDerivedTypes="True" TargetType="ContentPage">
        <Setter Property="BackgroundColor" Value="{StaticResource BackgroundColor}" />
    </Style>

    <!--  Label  -->
    <Style ApplyToDerivedTypes="True" TargetType="Label">
        <Setter Property="TextColor" Value="{StaticResource TextColor}" />
    </Style>
</ResourceDictionary>