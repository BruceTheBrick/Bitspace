<?xml version="1.0" encoding="utf-8" ?>
<ScrollView
    x:Class="Bitspace.Controls.PillList"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:Bitspace.Controls;assembly=Bitspace"
    x:Name="This"
    Orientation="Horizontal">
    <StackLayout BindableLayout.ItemsSource="{Binding ItemsSource, Source={x:Reference This}}">
        <BindableLayout.ItemTemplate>
            <DataTemplate x:DataType="controls:PillViewModel">
                <controls:Pill />
            </DataTemplate>
        </BindableLayout.ItemTemplate>
    </StackLayout>
</ScrollView>